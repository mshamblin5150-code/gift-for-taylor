name: schedule_rules
description: Domain rules for the ER Schedule behind one public interface.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.4

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  schedule_rules_testing:
    path: ../schedule_rules_testing
  lints: ^6.0.0
  test: ^1.25.6
